name: Deploy Wiki
on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
      - run: pip install mkdocs-material pillow cairosvg mkdocs-glightbox mkdocs-git-revision-date-localized-plugin mkdocs-roamlinks-plugin
      - run: mkdocs gh-deploy --force